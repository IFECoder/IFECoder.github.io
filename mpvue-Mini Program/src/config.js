// 配置项

//第一个为本地开发环境
// const host = 'http://localhost:5757'
const host = 'https://xpxmzpel.qcloud.la'

const config = {
  host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}
export default config
